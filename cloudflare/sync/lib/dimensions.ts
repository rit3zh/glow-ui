/**
 * Kept as the landing sync's import path; the reader itself is shared, since
 * `pull` and the preview tools want the same measurements.
 */
export { probeDimensions, type Dimensions } from "../../lib/dimensions";
